spring:
  application:
    name: tier-list-service
  data:
    mongodb:
      uri: mongodb://root:example@localhost:27017/tier_list_db?authSource=admin
  main:
    allow-bean-definition-overriding: true
    
server:
  port: ${SERVER_PORT:8082}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

# External Services
services:
  image-storage:
    url: ${IMAGE_SERVICE_URL:http://localhost:8084} 